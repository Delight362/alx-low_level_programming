#include "main.h"

/**
* _strlen - return the length of a string.
* @s: input string.
* Return: length of a string.
*/
int _strlen(char *5)
{
	in count = 0;

	while (*(s * count) != '\0')
		count++;
	return (count);
}
